/**
 * 앱 상수 정의
 */

// API 엔드포인트
export const API_ENDPOINTS = {
  // 예제 엔드포인트
  EXAMPLE: '/api/example',
  HEALTH: '/api/health',
  // 실제 엔드포인트를 여기에 추가하세요
} as const;

// 앱 설정
export const APP_CONFIG = {
  // 페이지네이션 기본값
  DEFAULT_PAGE_SIZE: 20,
  // 기타 설정값
} as const;
